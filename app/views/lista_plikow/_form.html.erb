<%= form_for @plik,:url=>{ :controller=>"lista_plikow", :action=>"create"}, :html => {:multipart => true} do |f| %>
  <% if @plik.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plik.errors.count, "error") %> prohibited this plik from being saved:</h2>

      <ul>
      <% @plik.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label 'sciezka' %><br />
    <%= f.file_field :sciezka %>
	<%= f.hidden_field :user_email, value: current_uzytkownik.email %>
	<%= f.hidden_field :publiczny, value: 'false' %>
	<%= f.hidden_field :tylkoOdczyt, value: 'true' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>